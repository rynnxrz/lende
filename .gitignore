# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results
/.playwright-cli/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
/output/pdf/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase local runtime state
/supabase/.temp/

# sensitive docs
rules.txt

# personal / temp files
interview-prep-*.md
*面试准备包*.md
saas-conversion-roadmap.md
lh.html
src/app/_test_mv.tmp
src/test_write_perm.tmp

# agent / tool artifacts
.agent/
.claude/
.venv-lookbook/
outputs/

# BRIEF-53 spike (real SKU snapshot + intermediate JPEGs — never commit)
/scripts/spike-vision-ocr/output/
/scripts/spike-vision-ocr/inventory-snapshot.json
# samples/ folder is a v1 artifact that mirrors docs/2026SS.pdf + docs/Ivy J Studio Pricing List.pdf
# (sandbox couldn't delete it during the v1→v2 retraction; safe to ignore + remove on next clean checkout)
/scripts/spike-vision-ocr/samples/
