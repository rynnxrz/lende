/**
 * BRIEF-53 v2 — DEPRECATED.
 *
 * Same retraction as `test-1-recognition.ts`. The 2026-05-08 spike's Test 2
 * matching results in `output/test-2-results.json` were produced inside the
 * Cowork session: the session read the Test 1 JSONs + `inventory-snapshot.json`
 * and applied PROMPT_T2 by reasoning, no SDK / API call.
 *
 * Do not run this file. Use a Cowork / Claude Code session per BRIEF-53 v2
 * Step 4 instead.
 */
export {};
